RewriteEngine On

# Hvis det ikke er en rigtig fil eller mappe, så send det til router.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ router.php [QSA,L]